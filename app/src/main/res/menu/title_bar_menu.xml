<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">


    <!-- "Tags", should appear as action button if possible -->
    <item
        android:id="@+id/action_tags"
        android:icon="@drawable/tag"
        android:title="@string/action_tag"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/submit_announcement"
        android:icon="@drawable/add_announcement"
        android:title="@string/action_add_announcement"
        app:showAsAction="ifRoom"/>

</menu>