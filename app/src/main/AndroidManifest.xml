<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.wofford.sykesda.wocoapptest">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission
        android:name="edu.wofford.sykesda.wocoapptest.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainMenuLoggedIn"
            android:label="@string/MainMenuLoggedIn_label" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".directory"
            android:label="@string/directory_label" />
        <activity
            android:name=".food_menu"
            android:label="@string/food_menu_label" />
        <activity
            android:name=".tv_guide"
            android:label="@string/tv_guide_label" />
        <activity
            android:name=".call_emergency_golf"
            android:label="@string/call_emergency_golf_label" />
        <activity
            android:name=".class_schedule"
            android:label="@string/class_schedule_label" />
        <activity android:name=".settings" />
        <activity android:name=".directoryExtraDetails" />
        <activity
            android:name=".terrier_bucks"
            android:label="@string/terrier_bucks_label" />
        <activity
            android:name=".ResidentAssistant"
            android:label="@string/ResidentAssistant_label" />
        <activity
            android:name=".daily_announcements"
            android:label="@string/daily_announcements_label" />
        <activity
            android:name=".daily_announcements_details"
            android:label="@string/daily_announcements_details_label" />
        <activity
            android:name=".daily_announce_tags_filtering"
            android:label="@string/daily_announce_tags_filtering_label" />
        <activity
            android:name=".campus_food_menu"
            android:label="@string/campus_food_menu_label" />
        <activity android:name=".cafes" />
        <!-- <activity android:name=".avi" />
        <activity android:name=".study" /> -->
        <activity android:name=".restaurants" />
        <activity android:name=".student_activity" />
        <activity android:name=".Restaurant_discount" />
        <activity android:name=".Movies_discount" />
        <activity android:name=".Hardware_discount" />
        <activity android:name=".Software_discounts" />
        <activity android:name=".Auto_discounts" />
        <activity android:name=".Shopping_discounts" />
        <activity android:name=".Entertainment_discounts" />
        <activity
            android:name=".WNumber"
            android:label="@string/WNumber_label" />
        <activity
            android:name=".WNumber_ScanMenu"
            android:label="@string/WNumber_ScanMenu_label"
            android:screenOrientation="portrait" />

        <meta-data
            android:name="com.google.android.gms.vision.DEPENDENCIES"
            android:value="barcode" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".WNumber_Scanner"
            android:screenOrientation="portrait" />
        <activity android:name=".WNumber_Manual" />
        <activity android:name=".laundryRooms"
            android:label="Laundry Rooms"></activity>
    </application>

</manifest>